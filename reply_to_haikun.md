# 回复 Haikun

你这轮意见很有用，第五段和第七段那两条我查了，**你是对的**，下面说一下原因和处理。

## 数字对不上——确认了，但不是数据没更新

两组数字都出自同一次 Run All，`fig_data_*.npz` 我核过了，没有陈旧数据。问题是**取整不可组合**。

`snr_at_ber` 是在 log10(BER) 上插值，返回浮点数。1e-4 那一列真实交点是：

```
classical 6.5366    gradient 6.1834    差 = 0.353  ->  正文写 0.35   对
表格显示   6.54              6.18       差 = 0.36   ->  你减出来      也对
```

两个数都没错，但**差值是在四舍五入之前算的，表格是取整之后显示的**，所以凑不起来。

## 第七段更严重，你说的"依旧不一致"没错

那张图里的 `0.15 dB` 是 `annotate_gap` 画上去的，用的是 `|5.9248 − 5.7761| = 0.1487`。而表格显示 5.92 和 5.78，一减是 0.14。

**这是图和表格在同一张海报上直接打架**，比第五段的正文-表格不一致更显眼。而且数字烤在 PNG 里，改文字没用，得重跑生成图的 cell。

## 为什么以前没暴露

第五部分原来是那张四曲线图。**图上读不出精确数字**，观众只能看曲线间距，正文给的差值是唯一来源，没有可对照的东西。

换成表格之后，那些数字**突然变得可以做减法了**，矛盾才显出来。所以你说"图换了但后面的数据没改"，判断是准的——换成表格改变了读者能核对的内容，而正文的差值没有相应重新推导。

## 处理方式

我试过表格加一位小数，**不行**：`6.308 − 6.183 = 0.125` 进位到 0.13，而真值 0.1245 舍到 0.12，还是差一位。取整永远不可组合。

所以改成：**交点算出来立刻取整一次，之后表格、正文、图内标注全部从这一套数推导**。这样按构造就不可能不一致。

改完之后：

| | 原来 | 现在 |
|---|---|---|
| AWGN gain | 0.37 / 0.39 / 0.35 | 0.37 / 0.39 / **0.36** |
| RL cost | 0.12 / 0.15 / 0.12 | 0.12 / **0.14** / **0.13** |
| RL vs classical | 0.24 | **0.23–0.25** |
| 图内标注 | 0.15 dB | **0.14 dB** |
| Conclusion | 0.12 to 0.15 | 0.12 to **0.14** |

代价是这几个数和 notebook 输出差 0.01 dB。但那些曲线本身的相对标准误是 **3.2%**，0.01 dB 远在测量不确定度以下——而海报能自己验算比和 notebook 完全一致更重要，因为观众手里只有海报。

## 其他几条

全部照你说的改了：

- 第一段 → `Research questions:` 加你写的那两个提问
- 第二段 → 按你的 Objectives / Contributions 拆分，公式移到 System Model（那里正好可以定义 R、L、P、N₀、ĥ，导师也要求了这个）
- 第三段 → `AWGN | Rayleigh` 的竖线改成 `or`
- 第五段 → 表格说明换成你写的 `Each estimate is based on at least 30 observed block errors`；`Fading costs` → `Rayleigh fading requires`；`(1+1/P)` 改成直接说"两个导频符号"，不再引用未定义的 P
- 第六段 → `cannot` → `should not`；标题 → `AWGN-Trained Weights`
- 第七段 → 标题 → `Training Without a Channel Model`

## 还需要你的

导师要求 conclusion 部分有 3–4 篇引用。现在 Next Steps 里 meta-learning 引了 [3]、OFDM 引了 [2]，**MMSE 均衡那条还没有出处**。你有读过合适的文献吗？必须是真读过的，展示时可能会被问到。
